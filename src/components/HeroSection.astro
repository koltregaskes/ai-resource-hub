---
interface Props {
  modelCount: number;
  lastUpdated: string;
}

const { modelCount, lastUpdated } = Astro.props;
---

<section class="relative overflow-hidden">
  <!-- Gradient background -->
  <div class="absolute inset-0 bg-gradient-to-b from-(--color-accent-glow) via-transparent to-transparent pointer-events-none" aria-hidden="true"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-24 relative">
    <div class="text-center max-w-3xl mx-auto">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-(--color-bg-tertiary) border border-(--color-border) text-xs text-(--color-text-secondary) mb-6">
        <span class="w-2 h-2 rounded-full bg-(--color-success) animate-pulse"></span>
        Tracking {modelCount} models &middot; Updated {lastUpdated}
      </div>

      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold tracking-tight mb-6">
        <span class="text-(--color-text-primary)">Compare AI Models.</span>
        <br />
        <span class="text-(--color-accent)">Find the Best Value.</span>
      </h1>

      <p class="text-lg sm:text-xl text-(--color-text-secondary) mb-10 max-w-2xl mx-auto leading-relaxed">
        Independent pricing, benchmarks, and value rankings for LLMs, image generators,
        video models, and more. No affiliate bias — just data.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a
          href="/compare/llm"
          class="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-(--color-accent) hover:bg-(--color-accent-hover) text-white font-semibold transition-colors accent-glow"
        >
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
          </svg>
          Compare LLMs
        </a>
        <a
          href="#categories"
          class="inline-flex items-center gap-2 px-6 py-3 rounded-lg border border-(--color-border) hover:border-(--color-border-hover) text-(--color-text-secondary) hover:text-(--color-text-primary) font-semibold transition-colors"
        >
          Explore All Categories
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>
