# AI Resource Hub Repo Instructions

Read `W:\Websites\AGENTS.md` first.

## Local Rules

- This is a public reference-first Astro site, not a generic content blog.
- Keep sources, methodology, and freshness claims honest.
- Shared pipeline logic belongs in `W:\Websites\shared\website-tools\` when it truly serves multiple sites.
- For `npm install`, `npm run dev`, and `npm run build`, follow the `C:\Workspaces` checkout rule in the Websites root `AGENTS.md`.
- Read `README.md` and `docs/DATA-COLLECTION-PLAN.md` before major changes.
